description: Credentials for the Miro REST API using an OAuth access token, to
  manage boards, their items and organization teams.
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    token:
      type: string
      description: Access token retrieved from the OAuth flow.
  required:
    - token
