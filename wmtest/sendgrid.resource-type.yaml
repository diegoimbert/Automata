description: 'Twilio SendGrid API key for the SendGrid v3 API: transactional
  email, contacts, marketing lists and bounces.'
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    token:
      type: string
      description: API token
      default: ''
  required:
    - token
