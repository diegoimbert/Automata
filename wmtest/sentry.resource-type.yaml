description: Sentry auth token for the Sentry API, scoped to an organization
  slug and data region (us or de).
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft-07/schema#
  type: object
  properties:
    organizationSlug:
      type: string
      description: Organization slug
    region:
      type: string
      description: The data-storage-location for an organization
      default: us
      enum:
        - us
        - de
    token:
      type: string
      description: Auth token
  required:
    - region
    - token
