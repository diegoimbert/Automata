description: Credentials for the YNAB (You Need A Budget) API using a personal access token.
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    token:
      type: string
  required:
    - token
