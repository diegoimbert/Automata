description: DigitalOcean personal access token, authorizing management of
  Droplets, Kubernetes clusters, databases and other cloud resources.
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    token:
      type: string
  required:
    - token
