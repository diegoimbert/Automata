description: Credentials for the Attio CRM API using an access token from the
  dashboard or the OAuth flow.
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    token:
      type: string
      description: Access token generated from the dashboard or the OAuth flow.
  required:
    - token
