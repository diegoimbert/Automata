display_name: Google Docs
description: Google OAuth token authorizing the Google Docs API for creating and
  editing documents.
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    token:
      type: string
      description: The OAuth token, refreshed if necessary
  required:
    - token
