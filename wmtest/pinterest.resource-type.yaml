description: OAuth access token for the Pinterest API v5, authorizing access to
  pins, boards, catalogs and ad accounts.
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    token:
      type: string
  required:
    - token
