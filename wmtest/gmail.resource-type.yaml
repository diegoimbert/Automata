description: Google OAuth token authorizing the Gmail API to send email as the
  connected Google account.
format_extension: null
is_fileset: false
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    token:
      type: string
      description: ''
  required:
    - token
